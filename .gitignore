# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.env

# Testing / coverage
.pytest_cache/
.coverage
htmlcov/
.ruff_cache/

# Project data + outputs (do not commit large/derived data)
data/
results/
*.csv
*.tsv
*.parquet
!config.yaml

# Raw TCGA download (BCR Biotab clinical, GDCdata, .RData snapshots) -- large,
# fetched locally via TCGAbiolinks/GDC, never committed. mirna_tcga.biotab reads
# it in place; config biotab.root points here.
TCGA/
*.RData

# Editors / OS
.idea/
.vscode/
.DS_Store

# Cached gene-set libraries (fetched at runtime)
genesets_cache/
*.gmt
